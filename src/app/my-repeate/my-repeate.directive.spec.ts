import { MyRepeateDirective } from './my-repeate.directive';

describe('MyRepeateDirective', () => {
  it('should create an instance', () => {
    const directive = new MyRepeateDirective();
    expect(directive).toBeTruthy();
  });
});
