import { MyConfirmDirective } from './my-confirm.directive';

describe('MyConfirmDirective', () => {
  it('should create an instance', () => {
    const directive = new MyConfirmDirective();
    expect(directive).toBeTruthy();
  });
});
